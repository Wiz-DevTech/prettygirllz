package com.socialcommerce.moderation.domain;

/**
 * Enum representing content moderation status
 * Defines various states of content review
 */
public enum ModerationStatus {
    PENDING, APPROVED, REJECTED, FLAGGED, UNDER_REVIEW
}
